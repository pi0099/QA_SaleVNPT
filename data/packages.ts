/**
 * Production package sections — canonical source is lib/data.ts (defaultSections).
 * Do not duplicate or modify prices here.
 */
export { defaultSections as packageSections } from "@/lib/data";
export type { PackageCard, PackageSection } from "@/lib/data";
